<UserControl x:Class="DS4WinWPF.DS4Forms.ColorPicker_UserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DS4WinWPF.DS4Forms"
             mc:Ignorable="d" Width="481" Height="296"
             Background="{DynamicResource BackgroundColor}">
    <UniformGrid Rows="3" Margin="20">

        <Canvas x:Name="ColorSpectrum" Width="250" Height="50" Cursor="Cross"
                MouseDown="ColorSpectrum_MouseDown" MouseMove="ColorSpectrum_MouseMove" MouseUp="ColorSpectrum_MouseUp">
            <Border Width="250" Height="50" CornerRadius="4" BorderThickness="0">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                        <GradientStop Color="#FF0000" Offset="0.0"/>
                        <GradientStop Color="#FFFF00" Offset="0.16"/>
                        <GradientStop Color="#00FF00" Offset="0.33"/>
                        <GradientStop Color="#00FFFF" Offset="0.5"/>
                        <GradientStop Color="#0000FF" Offset="0.66"/>
                        <GradientStop Color="#FF00FF" Offset="0.83"/>
                        <GradientStop Color="#FF0000" Offset="1.0"/>
                    </LinearGradientBrush>
                </Border.Background>
            </Border>

            <Ellipse x:Name="ColorThumb" Width="12" Height="12" Stroke="White" StrokeThickness="2" Fill="Transparent"
                     Canvas.Left="20" Canvas.Top="20">
                <Ellipse.Effect>
                    <DropShadowEffect BlurRadius="3" Color="Black" ShadowDepth="1" Opacity="0.6"/>
                </Ellipse.Effect>
            </Ellipse>
        </Canvas>

        <Slider x:Name="BrightnessSlider" Orientation="Horizontal" 
                Minimum="0" Maximum="1" Value="1"
                ValueChanged="BrightnessSlider_ValueChanged">
            <Slider.Template>
                <ControlTemplate TargetType="Slider">
                    <Grid>
                        <Border x:Name="TrackBorder" Width="14" CornerRadius="7" BorderBrush="#333333" BorderThickness="1">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                    <GradientStop x:Name="BrightnessTopColor" Color="Transparent" Offset="0.0"/>
                                    <GradientStop Color="Black" Offset="1.0"/>
                                </LinearGradientBrush>
                            </Border.Background>
                        </Border>
                        <Track x:Name="PART_Track">
                            <Track.Thumb>
                                <Thumb Width="20" Height="10" Cursor="SizeNS">
                                    <Thumb.Template>
                                        <ControlTemplate>
                                            <Rectangle Fill="White" Stroke="#333333" StrokeThickness="1" RadiusX="2" RadiusY="2"/>
                                        </ControlTemplate>
                                    </Thumb.Template>
                                </Thumb>
                            </Track.Thumb>
                        </Track>
                    </Grid>
                </ControlTemplate>
            </Slider.Template>
        </Slider>

        <StackPanel Grid.Column="2" Margin="20,0,0,0" Orientation="Horizontal" HorizontalAlignment="Left">
            <TextBlock Text="Selected Color" Foreground="#AAAAAA" FontSize="12" Margin="0,0,0,5"/>

            <Border x:Name="ColorPreview" Width="80" Height="80" CornerRadius="8" 
                    Background="Red" BorderBrush="#333333" BorderThickness="1" HorizontalAlignment="Left"/>

            <TextBox x:Name="HexDisplay" Width="80" Padding="4" IsReadOnly="True"
                     Background="#2D2D2D" Foreground="White" BorderThickness="0" TextAlignment="Center" FontSize="13"/>
        </StackPanel>
    </UniformGrid>
</UserControl>
