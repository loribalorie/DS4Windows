<Window x:Class="DS4WinWPF.DS4Forms.WelcomeDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:lex="http://wpflocalizeextension.codeplex.com"
        xmlns:local="clr-namespace:DS4WinWPF.DS4Forms"
        lex:LocalizeDictionary.DesignCulture=""
        lex:ResxLocalizationProvider.DefaultAssembly="DS4Windows"
        lex:ResxLocalizationProvider.DefaultDictionary="Strings"
        mc:Ignorable="d"
        Title="{lex:Loc Welcome.WinTitle}"
        Height="800"
        Width="800"
        d:Height="Auto"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource BackgroundColor}"
        UseLayoutRounding="True">
    <ScrollViewer VerticalScrollBarVisibility="Auto">

        <ScrollViewer.Resources>
            <Style TargetType="{x:Type Button}" BasedOn="{StaticResource {x:Type Button}}">
                <Setter Property="Width" Value="Auto"/>
                <Setter Property="Height" Value="40"/>
            </Style>
            <Style TargetType="{x:Type TextBlock}" x:Key="CommonMarginTxt" BasedOn="{StaticResource {x:Type TextBlock}}">
                <Setter Property="Margin" Value="0,10,0,0" />
            </Style>

            <Style TargetType="GroupBox" BasedOn="{StaticResource {x:Type GroupBox}}">
                <Setter Property="Margin" Value="10"/>
            </Style>

            <local:WelcomeDialogResourcePaths x:Key="resourcePaths" />
        </ScrollViewer.Resources>
        <StackPanel Orientation="Vertical" Margin="10" Width="640">

            <StackPanel>
                <Label Content="DS4Windows Setup" FontSize="16" FontWeight="Medium"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <GroupBox Header="Step 1" Width="300" Height="Auto">
                    <StackPanel Orientation="Vertical">
                        <Button x:Name="vigemInstallBtn" Content="{lex:Loc Welcome.Step1Text}" DockPanel.Dock="Top" Click="VigemInstallBtn_Click"/>

                        <TextBlock TextAlignment="Center" TextWrapping="Wrap" Margin="20 5">
                            <TextBlock.Text>
                                <lex:Loc Key="Welcome.Step1HelpText" />
                            </TextBlock.Text>
                        </TextBlock>
                    </StackPanel>
                </GroupBox>

                <GroupBox Header="Step 2" Width="300">
                    <StackPanel Orientation="Vertical">
                        <Button x:Name="step2Btn" IsEnabled="False" Content="{lex:Loc Welcome.Step2Text}" Click="Step2Btn_Click" />
                        <TextBlock TextAlignment="Center" TextWrapping="Wrap" Margin="20 5">
                            <TextBlock.Text>
                                <lex:Loc Key="Welcome.Step2HelpText" />
                            </TextBlock.Text>
                        </TextBlock>
                    </StackPanel>
                </GroupBox>
            </StackPanel>
            
            <GroupBox Header="Step 3">
                <StackPanel Orientation="Horizontal" Margin="8 0">

                    <StackPanel Orientation="Vertical" Width="280">
                        <TextBlock TextWrapping="Wrap"
                               TextAlignment="Left"
                               Text="{lex:Loc Welcome.Step3HelpText}">
                        </TextBlock>

                        <TextBlock TextAlignment="Left" TextWrapping="Wrap" Margin="0,10,0,0">
                            <Run Text="{lex:Loc Welcome.Step3BluetoothHelpText}" />
                        </TextBlock>
                    </StackPanel>
                    <StackPanel VerticalAlignment="Center" Margin="40 0" HorizontalAlignment="Center">
                        <Image Width="230" Height="130" Source="/Resources/Pairmode.png"/>
                        <TextBlock TextAlignment="Center">
                        <Hyperlink x:Name="bluetoothSetLink" Click="BluetoothSetLink_Click" Foreground="{DynamicResource AccentColor}">
                            <Run Text="{lex:Loc Welcome.BluetoothSettings}" />
                        </Hyperlink>
                        </TextBlock>
                    </StackPanel>
                </StackPanel>
            </GroupBox>

            <GroupBox Header="Step 4 - Optional">
                <StackPanel x:Name="step4HidHidePanel">
                    <Button x:Name="hidHideInstallBtn" Content="{lex:Loc Welcome.Step4Text}" Click="HidHideInstall_Click" />
                    <Expander Header="Description" Margin="0 8">
                        <TextBlock TextWrapping="Wrap" Margin="8">
                        <TextBlock.Text>
                            <lex:Loc Key="Welcome.Step4HelpText" />
                        </TextBlock.Text>
                    </TextBlock>
                        </Expander>
                </StackPanel>
            </GroupBox>

            <GroupBox Header="Step 5 - Optional">
                <StackPanel x:Name="step5FakerInputPanel" DockPanel.Dock="Top" Margin="0,8,0,0">
                    <Button x:Name="fakerInputInstallBtn"
                        Content="{lex:Loc Welcome.Step5Text}"
                        Click="FakerInputInstallBtn_Click" />
                    <Expander Header="Description" Margin="0 8">
                        <TextBlock TextWrapping="Wrap" Margin="8">
                            <TextBlock.Text>
                                <lex:Loc Key="Welcome.Step5HelpText" />
                            </TextBlock.Text>
                        </TextBlock>
                    </Expander>
                </StackPanel>
            </GroupBox>

            <Button x:Name="finishedBtn" Style="{DynamicResource ImportantButtonStyle}"
                    Content="Finish" HorizontalAlignment="Center" Click="FinishedBtn_Click" Width="120" Height="32" Margin="0,8" FontWeight="Bold" FontSize="14"/>
        </StackPanel>
    </ScrollViewer>
</Window>
