<UserControl x:Class="DS4WinWPF.DS4Forms.DupBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DS4WinWPF.DS4Forms"
             xmlns:lex="http://wpflocalizeextension.codeplex.com"
             lex:LocalizeDictionary.DesignCulture=""
             lex:ResxLocalizationProvider.DefaultAssembly="DS4Windows"
             lex:ResxLocalizationProvider.DefaultDictionary="Strings"
             mc:Ignorable="d" Height="30" Width="500">
    <DockPanel>
        <Grid DockPanel.Dock="Left"  Margin="10,0,0,0">
            
            <TextBox x:Name="profileTxt" TextWrapping="Wrap" Text=""
                     Height="24" Width="250" HorizontalAlignment="Left"
                 VerticalContentAlignment="Center" MaxLines="1"/>

            <TextBlock Height="24" Width="130" Margin="6" HorizontalAlignment="Left"
                       Text="{Binding Source={lex:Loc Resources:TypeNewName}, StringFormat='&lt;{0}&gt;'}"
                       Foreground="{DynamicResource ForegroundColor}" Opacity="0.7" IsHitTestVisible="False">
                <TextBlock.Style>

                    <Style TargetType="TextBlock">
                        
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ElementName=profileTxt, Path=Text.IsEmpty, Mode=OneWay}" Value="False">
                                <Setter Property="Visibility" Value="Hidden" />
                            </DataTrigger>

                            <DataTrigger Binding="{Binding ElementName=profileTxt, Path=Text.IsEmpty, Mode=OneWay}" Value="True">
                                <Setter Property="Visibility" Value="Visible" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
        </Grid>

        <StackPanel Orientation="Horizontal" Margin="6,0,6,0"
                    HorizontalAlignment="Left" VerticalAlignment="Stretch">
            <Button x:Name="saveBtn" Content="{lex:Loc Save}" Width="80" Click="SaveBtn_Click" />
            <Button x:Name="cancelBtn" Content="{lex:Loc Cancel}" Width="80" Click="CancelBtn_Click" Margin="6,0,0,0" />
        </StackPanel>
    </DockPanel>
</UserControl>
