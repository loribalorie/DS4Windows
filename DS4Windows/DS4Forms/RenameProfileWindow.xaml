<Window x:Class="DS4WinWPF.DS4Forms.RenameProfileWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DS4WinWPF.DS4Forms"
        xmlns:lex="http://wpflocalizeextension.codeplex.com"
        lex:LocalizeDictionary.DesignCulture=""
        lex:ResxLocalizationProvider.DefaultAssembly="DS4Windows"
        lex:ResxLocalizationProvider.DefaultDictionary="Strings"
        mc:Ignorable="d"
        Title="{lex:Loc RenameProfileWin.WinTitle}"
        Height="200" Width="300"
        Style="{DynamicResource WindowStyle}"
        WindowStartupLocation="CenterOwner"
        Closed="Window_Closed"
        UseLayoutRounding="True">
    
    <DockPanel x:Name="mainDockPanel" Margin="8" LastChildFill="True">
        
        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Height="30" Margin="0 8 0 0">
            <Button x:Name="cancelBtn" Content="{lex:Loc Cancel}" Click="CancelBtn_Click" />
            <Button x:Name="confirmBtn" Content="{lex:Loc Confirm}" Click="ConfirmBtn_Click" Margin="8,0,0,0" />
        </StackPanel>

        <Border Style="{DynamicResource BorderStyle}" Padding="16">
            <StackPanel Orientation="Vertical">

                <TextBlock DockPanel.Dock="Top"
                       TextWrapping="Wrap"
                       Text="{lex:Loc RenameProfileWin.TypeNewProfileName}"/>
                <TextBox DockPanel.Dock="Bottom"
                         Height="24"
                         Text="{Binding ProfileName}" Margin="0 16"/>

            </StackPanel>
        </Border>
    </DockPanel>
</Window>
