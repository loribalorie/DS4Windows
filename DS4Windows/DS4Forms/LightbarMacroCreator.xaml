<Window x:Class="DS4WinWPF.DS4Forms.LightbarMacroCreator"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        xmlns:lex="http://wpflocalizeextension.codeplex.com"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:DS4WinWPF.DS4Forms"
        lex:LocalizeDictionary.DesignCulture=""
        lex:ResxLocalizationProvider.DefaultAssembly="DS4Windows"
        lex:ResxLocalizationProvider.DefaultDictionary="Strings"
        mc:Ignorable="d"
        Background="{DynamicResource BackgroundColor}"
        Style="{DynamicResource WindowStyle}"
        WindowStartupLocation="CenterOwner"
        Title="Lightbar macro creator"
        MinHeight="450" MinWidth="800"
        UseLayoutRounding="True">
    
    <Grid Margin="8">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="4*" />
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <Border Style="{DynamicResource BorderStyle}">
            <ListBox x:Name="MacroListBox" ItemsSource="{Binding LightbarMacro.Macro}" SelectionMode="Single" BorderThickness="0"
                     Background="Transparent"/>
        </Border>

        <StackPanel Grid.Column="1" Margin="8 0 0 0">

            <GroupBox Header="{lex:Loc ColorAndLength}" Margin="0 0 0 8">
                <StackPanel Orientation="Horizontal" Margin="8 10" VerticalAlignment="Center">
                    <Button Click="SelectColor_Click" Background="{Binding CurrentColorString}" Height="30" Width="30" />
                    <xctk:UIntegerUpDown Value="{Binding CurrentInterval}"  Width="100" Increment="10" Margin="8 0" 
                                         FormatString="{}{0} ms"/>
                    <!--<Label Content="ms" />-->
                </StackPanel>
            </GroupBox>
            
            
            <GroupBox Header="{lex:Loc Trigger}" Margin="0 0 0 8">
                <StackPanel Orientation="Vertical">
                    <StackPanel Margin="0 8 0 0">
                        <ComboBox x:Name="TriggerCombo" SelectedItem="{Binding LightbarMacro.Trigger}" Width="Auto"/>
                    </StackPanel>


                    <StackPanel Orientation="Horizontal" Margin="0 8 0 0">
                        <CheckBox IsChecked="{Binding LightbarMacro.CancelCurrent}" Content="{lex:Loc CancelCurrentMacro}">
                            <CheckBox.ToolTip>
                                <TextBlock Text="{lex:Loc CancelCurrentMacroTooltip}"/>
                            </CheckBox.ToolTip>
                        </CheckBox>

                    </StackPanel>
                </StackPanel>
            </GroupBox>


            <StackPanel Orientation="Vertical">
                <Button Content="{lex:Loc Add}" Click="AddColor_OnClick" Margin="8 4" Width="Auto"/>
                <Button Content="{lex:Loc Delete}" Click="DeleteColor_OnClick" Margin="8 4" Width="Auto"/>
                <Button Content="{lex:Loc Clear}" Click="Clear_OnClick" Margin="8 4" Width="Auto"/>
                <Button Content="{lex:Loc Save}" Click="Save_OnClick" Margin="8 4" Width="Auto" Style="{DynamicResource ImportantButtonStyle}"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
