<Window x:Class="DS4WinWPF.DS4Forms.ChangelogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:lex="http://wpflocalizeextension.codeplex.com"
        xmlns:mdxam="clr-namespace:MdXaml;assembly=MdXaml"
        xmlns:local="clr-namespace:DS4WinWPF.DS4Forms"
        mc:Ignorable="d"
        Title="{lex:Loc Changelog}" Height="450" Width="800"
        WindowStartupLocation="CenterOwner"
        UseLayoutRounding="True"
        Style="{DynamicResource WindowStyle}">
    
    <DockPanel Margin="8" Background="{DynamicResource BackgroundColor}">
        
        
        <TextBlock x:Name="captionTextBlock"
                   Text="{Binding IntroText}"
                   DockPanel.Dock="Top" Margin="8 8 8 0"
                   FontWeight="Medium" FontSize="15"/>
        
        <StackPanel Orientation="Horizontal"
                    DockPanel.Dock="Bottom"
                    HorizontalAlignment="Right">
            <Button x:Name="closeBtn"
                    Content="{lex:Loc Close}"
                    Click="CloseBtn_Click" />
        </StackPanel>

        <Border Style="{DynamicResource BorderStyle}" Margin="0 8">
            <mdxam:MarkdownScrollViewer BorderBrush="{StaticResource BorderColor}"
                                    Foreground="{DynamicResource ForegroundColor}"
                                    Markdown="{Binding Markdown}"
                                    ClickAction="OpenBrowser"
                                    MarkdownStyle="{StaticResource DocumentStyleGithubLike}"
                                        Margin="-5"/>
        </Border>
    </DockPanel>
</Window>
