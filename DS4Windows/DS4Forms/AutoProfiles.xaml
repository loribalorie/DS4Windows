<UserControl x:Class="DS4WinWPF.DS4Forms.AutoProfiles"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:lex="http://wpflocalizeextension.codeplex.com"
             lex:LocalizeDictionary.DesignCulture=""
             lex:ResxLocalizationProvider.DefaultAssembly="DS4Windows"
             lex:ResxLocalizationProvider.DefaultDictionary="Strings"
             xmlns:local="clr-namespace:DS4WinWPF.DS4Forms"
             xmlns:entity="clr-namespace:DS4WinWPF"
             mc:Ignorable="d" 
             d:DesignWidth="800"
             d:Height="Auto"
             UseLayoutRounding="True"
             Background="{DynamicResource BackgroundColor}">

    <DockPanel Margin="8 8 0 8">
        <ScrollViewer x:Name="sidebarScrollViewer" Width="270" VerticalScrollBarVisibility="Auto" DockPanel.Dock="Right">
            <StackPanel x:Name="outerEditControlsPanel" Margin="0 0 8 0">
                <StackPanel.Resources>
                    <CollectionViewSource x:Key="profileCollection" Source="{Binding ProfileList.ProfileListCol,Mode=OneWay}" />
                </StackPanel.Resources>
                <StackPanel x:Name="editControlsPanel" IsEnabled="False" d:IsEnabled="True">
                    <DockPanel HorizontalAlignment="Stretch" Margin="0">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <Button x:Name="removeAutoBtn" Content="{lex:Loc Remove}" IsEnabled="{Binding Exists}" Click="RemoveAutoBtn_Click" Margin="4 0" Width="100"/>
                            <Button x:Name="saveAutoBtn" Content="{lex:Loc Save}" Click="SaveAutoBtn_Click" Margin="4 0" Width="100" Style="{DynamicResource ImportantButtonStyle}"/>
                        </StackPanel>
                    </DockPanel>

                    <Border Style="{DynamicResource BorderStyle}" Margin="8 4 0 0">
                        <Grid x:Name="autoProfilesGrid" >
                            <Grid.RowDefinitions>
                                <RowDefinition Height="30"/>
                                <RowDefinition Height="30"/>
                                <RowDefinition Height="30"/>
                                <RowDefinition Height="30"/>
                                <RowDefinition Height="30"/>
                                <RowDefinition Height="30"/>
                                <RowDefinition Height="30"/>
                                <RowDefinition Height="30"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="80"/>
                                <ColumnDefinition/>
                            </Grid.ColumnDefinitions>

                            <Label Content="{lex:Loc Controller1Text}" />
                            <Label Content="{lex:Loc Controller2Text}" Grid.Row="1"/>
                            <Label Content="{lex:Loc Controller3Text}" Grid.Row="2"/>
                            <Label Content="{lex:Loc Controller4Text}" Grid.Row="3"/>
                            <Label Content="{lex:Loc Controller5Text}" Grid.Row="4" Visibility="{Binding ExpandedControllersVisible, Mode=OneWay}" />
                            <Label Content="{lex:Loc Controller6Text}" Grid.Row="5" Visibility="{Binding ExpandedControllersVisible, Mode=OneWay}" />
                            <Label Content="{lex:Loc Controller7Text}" Grid.Row="6" Visibility="{Binding ExpandedControllersVisible, Mode=OneWay}"/>
                            <Label Content="{lex:Loc Controller8Text}" Grid.Row="7" Visibility="{Binding ExpandedControllersVisible, Mode=OneWay}" />

                            <ComboBox x:Name="cont1AutoProf" Grid.Column="1" SelectedIndex="{Binding SelectedIndexCon1}" Width="Auto">
                                <ComboBox.Resources>
                                    <DataTemplate DataType="{x:Type ComboBoxItem}">
                                        <TextBlock Text="{Binding Content}" />
                                    </DataTemplate>

                                    <DataTemplate DataType="{x:Type entity:ProfileEntity}">
                                        <TextBlock Text="{Binding Name}" />
                                    </DataTemplate>
                                </ComboBox.Resources>
                                <ComboBox.ItemsSource>
                                    <CompositeCollection>
                                        <ComboBoxItem Content="(none)"/>
                                        <CollectionContainer x:Name="cont1AutoProfCol" Collection="{Binding Source={StaticResource profileCollection}}" />
                                    </CompositeCollection>
                                </ComboBox.ItemsSource>
                            </ComboBox>

                            <ComboBox x:Name="cont2AutoProf" Grid.Row="1" Grid.Column="1" SelectedIndex="{Binding SelectedIndexCon2}" Width="Auto">
                                <ComboBox.Resources>
                                    <DataTemplate DataType="{x:Type ComboBoxItem}">
                                        <TextBlock Text="{Binding Content}" />
                                    </DataTemplate>

                                    <DataTemplate DataType="{x:Type entity:ProfileEntity}">
                                        <TextBlock Text="{Binding Name}" />
                                    </DataTemplate>
                                </ComboBox.Resources>
                                <ComboBox.ItemsSource>
                                    <CompositeCollection>
                                        <ComboBoxItem Content="(none)"/>
                                        <CollectionContainer x:Name="cont2AutoProfCol" Collection="{Binding Source={StaticResource profileCollection}}" />
                                    </CompositeCollection>
                                </ComboBox.ItemsSource>
                            </ComboBox>

                            <ComboBox x:Name="cont3AutoProf" Grid.Row="2" Grid.Column="1" SelectedIndex="{Binding SelectedIndexCon3}" Width="Auto">
                                <ComboBox.Resources>
                                    <DataTemplate DataType="{x:Type ComboBoxItem}">
                                        <TextBlock Text="{Binding Content}" />
                                    </DataTemplate>

                                    <DataTemplate DataType="{x:Type entity:ProfileEntity}">
                                        <TextBlock Text="{Binding Name}" />
                                    </DataTemplate>
                                </ComboBox.Resources>
                                <ComboBox.ItemsSource>
                                    <CompositeCollection>
                                        <ComboBoxItem Content="(none)"/>
                                        <CollectionContainer x:Name="cont3AutoProfCol" Collection="{Binding Source={StaticResource profileCollection}}" />
                                    </CompositeCollection>
                                </ComboBox.ItemsSource>
                            </ComboBox>

                            <ComboBox x:Name="cont4AutoProf" Grid.Row="3" Grid.Column="1" SelectedIndex="{Binding SelectedIndexCon4}" Width="Auto">
                                <ComboBox.Resources>
                                    <DataTemplate DataType="{x:Type ComboBoxItem}">
                                        <TextBlock Text="{Binding Content}" />
                                    </DataTemplate>

                                    <DataTemplate DataType="{x:Type entity:ProfileEntity}">
                                        <TextBlock Text="{Binding Name}" />
                                    </DataTemplate>
                                </ComboBox.Resources>
                                <ComboBox.ItemsSource>
                                    <CompositeCollection>
                                        <ComboBoxItem Content="(none)"/>
                                        <CollectionContainer x:Name="cont4AutoProfCol" Collection="{Binding Source={StaticResource profileCollection}}" />
                                    </CompositeCollection>
                                </ComboBox.ItemsSource>
                            </ComboBox>

                            <ComboBox x:Name="cont5AutoProf" Grid.Row="4" Grid.Column="1" Visibility="{Binding ExpandedControllersVisible, Mode=OneWay}" 
                                      SelectedIndex="{Binding SelectedIndexCon5}" Width="Auto">
                                <ComboBox.Resources>
                                    <DataTemplate DataType="{x:Type ComboBoxItem}">
                                        <TextBlock Text="{Binding Content}" />
                                    </DataTemplate>

                                    <DataTemplate DataType="{x:Type entity:ProfileEntity}">
                                        <TextBlock Text="{Binding Name}" />
                                    </DataTemplate>
                                </ComboBox.Resources>
                                <ComboBox.ItemsSource>
                                    <CompositeCollection>
                                        <ComboBoxItem Content="(none)"/>
                                        <CollectionContainer Collection="{Binding Source={StaticResource profileCollection}}" />
                                    </CompositeCollection>
                                </ComboBox.ItemsSource>
                            </ComboBox>

                            <ComboBox x:Name="cont6AutoProf" Grid.Row="5" Grid.Column="1" Visibility="{Binding ExpandedControllersVisible, Mode=OneWay}"
                                      SelectedIndex="{Binding SelectedIndexCon6}" Width="Auto">
                                <ComboBox.Resources>
                                    <DataTemplate DataType="{x:Type ComboBoxItem}">
                                        <TextBlock Text="{Binding Content}" />
                                    </DataTemplate>

                                    <DataTemplate DataType="{x:Type entity:ProfileEntity}">
                                        <TextBlock Text="{Binding Name}" />
                                    </DataTemplate>
                                </ComboBox.Resources>
                                <ComboBox.ItemsSource>
                                    <CompositeCollection>
                                        <ComboBoxItem Content="(none)"/>
                                        <CollectionContainer Collection="{Binding Source={StaticResource profileCollection}}" />
                                    </CompositeCollection>
                                </ComboBox.ItemsSource>
                            </ComboBox>

                            <ComboBox x:Name="cont7AutoProf" Grid.Row="6" Grid.Column="1" Visibility="{Binding ExpandedControllersVisible, Mode=OneWay}" 
                                      SelectedIndex="{Binding SelectedIndexCon7}" Width="Auto">
                                <ComboBox.Resources>
                                    <DataTemplate DataType="{x:Type ComboBoxItem}">
                                        <TextBlock Text="{Binding Content}" />
                                    </DataTemplate>

                                    <DataTemplate DataType="{x:Type entity:ProfileEntity}">
                                        <TextBlock Text="{Binding Name}" />
                                    </DataTemplate>
                                </ComboBox.Resources>
                                <ComboBox.ItemsSource>
                                    <CompositeCollection>
                                        <ComboBoxItem Content="(none)"/>
                                        <CollectionContainer Collection="{Binding Source={StaticResource profileCollection}}" />
                                    </CompositeCollection>
                                </ComboBox.ItemsSource>
                            </ComboBox>

                            <ComboBox x:Name="cont8AutoProf" Grid.Row="7" Grid.Column="1" Visibility="{Binding ExpandedControllersVisible, Mode=OneWay}" 
                                      SelectedIndex="{Binding SelectedIndexCon8}" Width="Auto">
                                <ComboBox.Resources>
                                    <DataTemplate DataType="{x:Type ComboBoxItem}">
                                        <TextBlock Text="{Binding Content}" />
                                    </DataTemplate>

                                    <DataTemplate DataType="{x:Type entity:ProfileEntity}">
                                        <TextBlock Text="{Binding Name}" />
                                    </DataTemplate>
                                </ComboBox.Resources>
                                <ComboBox.ItemsSource>
                                    <CompositeCollection>
                                        <ComboBoxItem Content="(none)"/>
                                        <CollectionContainer Collection="{Binding Source={StaticResource profileCollection}}" />
                                    </CompositeCollection>
                                </ComboBox.ItemsSource>
                            </ComboBox>
                        </Grid>
                    </Border>

                    <Border Style="{DynamicResource BorderStyle}" Margin=" 8 4 0 0">
                        <CheckBox IsChecked="{Binding Turnoff}" Height="Auto">
                            <CheckBox.Content>
                                <TextBlock Text="{lex:Loc TurnOffDS4WindowsTemporarily}" TextWrapping="Wrap"/>
                            </CheckBox.Content>
                        </CheckBox>
                    </Border>

                    <GroupBox Header="Path" Margin=" 8 4 0 0">
                        <TextBox Text="{Binding Path}" MinHeight="24" VerticalContentAlignment="Center" TextWrapping="Wrap"
                                     d:Text="But now I know, I've been sleeping on my own..." Padding="4 8">
                            <TextBox.ToolTip>
                                <TextBlock Text="{lex:Loc AutoProfPathTip}" VerticalAlignment="Center"/>
                            </TextBox.ToolTip>
                        </TextBox>
                    </GroupBox>

                    <GroupBox Header="Window Title" Margin=" 8 4 0 0">
                        <Grid>
                            <TextBox x:Name="mainTxtBox" Text="{Binding Title, UpdateSourceTrigger=LostFocus}" MinHeight="24" VerticalContentAlignment="Center" TextWrapping="Wrap"
                                     d:Text="And if you think that I'm still holding on to something..." Padding="4 8">
                                <TextBox.ToolTip>
                                    <TextBlock Text="{lex:Loc AutoProfTitleTip}"/>
                                </TextBox.ToolTip>
                            </TextBox>
                        </Grid>
                    </GroupBox>
                </StackPanel>

                <Border Style="{DynamicResource BorderStyle}" Margin=" 8 4 0 0">
                    <CheckBox x:Name="revertDefaultProfileOnUnknownCk" Height="Auto"
                                IsChecked="{Binding RevertDefaultProfileOnUnknown}">
                        <TextBlock Text="{lex:Loc RevertToDefaultProfileOnUnknown}" TextWrapping="Wrap"/>
                    </CheckBox>
                </Border>

                <Border Style="{DynamicResource BorderStyle}" Margin=" 8 4 0 0">
                    <CheckBox x:Name="showAutoDebugCk" Click="ShowAutoDebugCk_Click" Height="Auto">
                        <TextBlock Text="{lex:Loc ShowAutoProfileDebugMessages}" TextWrapping="Wrap"/>
                    </CheckBox>
                </Border>

                <GroupBox Header="{lex:Loc AutoProfiles.DisplaySwitchVia}"  Margin=" 8 4 0 0">
                    <StackPanel Orientation="Vertical">
                        <ComboBox ItemsSource="{Binding DisplayProfileSwitchList}" DisplayMemberPath="DisplayName" SelectedValuePath="ChoiceValue" SelectedValue="{Binding ProfileSwitchChoice}" Width="Auto"/>
                    </StackPanel>
                </GroupBox>
            </StackPanel>
        </ScrollViewer>

        <DockPanel Width="Auto" LastChildFill="True">
            <StackPanel Orientation="Horizontal" Margin="0 0 0 4" DockPanel.Dock="Top">
                <Button x:Name="addProgramsBtn" Content="{lex:Loc AddPrograms}" Click="AddProgramsBtn_Click" Margin="4 0" Width="150" Style="{DynamicResource ImportantButtonStyle}">
                    <Button.ContextMenu>
                        <ContextMenu Margin="8">
                            <MenuItem x:Name="steamMenuItem" Header="{lex:Loc AddSteamGames}" Click="SteamMenuItem_Click" />
                            <MenuItem x:Name="startMenuItem" Header="{lex:Loc AddStartMenuPrograms}" Click="StartMenuItem_Click" />
                            <MenuItem x:Name="browseProgsMenuItem" Header="{lex:Loc AddDirectory}" Click="BrowseProgsMenuItem_Click" />
                            <MenuItem x:Name="browseAddProgMenuItem" Header="{lex:Loc BrowseOtherPrograms}" Click="BrowseAddProgMenuItem_Click" />
                        </ContextMenu>
                    </Button.ContextMenu>
                </Button>
                <Button x:Name="hideUncheckedBtn" Content="{lex:Loc HideUnchecked}" Margin="4 0" Click="HideUncheckedBtn_Click" Width="150"/>
            </StackPanel>

            <Border Style="{DynamicResource BorderStyle}" DockPanel.Dock="Top" Padding="0 4">
                <ListView x:Name="programListLV" ScrollViewer.HorizontalScrollBarVisibility="Auto" ScrollViewer.CanContentScroll="True"
                          SelectedIndex="{Binding SelectedIndex}" SelectedItem="{Binding SelectedItem}" BorderThickness="0" Margin="0">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="{lex:Loc Name}" Width="150">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Height="30">
                                            <CheckBox IsChecked="{Binding Exists,Mode=OneWay}" IsEnabled="False" Style="{DynamicResource CheckBoxStyle_Default}" VerticalContentAlignment="Center"/>
                                            <Image Source="{Binding Exeicon}" Width="24" Margin="4 0" />
                                            <TextBlock Text="{Binding Filename}" VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <!--<GridViewColumn Header="{lex:Loc Path}" Width="150" DisplayMemberBinding="{Binding Path}">-->
                            <GridViewColumn Header="{lex:Loc Path}" Width="150">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <Grid Height="30">
                                            <TextBlock Text="{Binding Path}" VerticalAlignment="Center"/>
                                        </Grid>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <!--<GridViewColumn Header="{lex:Loc WindowTitle}" Width="150" DisplayMemberBinding="{Binding Title}">-->
                            <GridViewColumn Header="{lex:Loc WindowTitle}" Width="150">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <Grid Height="30">
                                            <TextBlock Text="{Binding Title}" VerticalAlignment="Center"/>
                                        </Grid>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                        </GridView>
                    </ListView.View>
                    
                    <ListView.ContextMenu>
                        <ContextMenu>
                            <MenuItem Header="{lex:Loc MoveUp}" Name="MoveUp" Click="MoveUpDownAutoBtn_Click"/>
                            <MenuItem Header="{lex:Loc MoveDown}" Name="MoveDown" Click="MoveUpDownAutoBtn_Click"/>
                        </ContextMenu>
                    </ListView.ContextMenu>
                </ListView>
            </Border>
        </DockPanel>
    </DockPanel>
</UserControl>
